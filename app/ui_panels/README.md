# `app/ui_panels/` (UI panels)

Streamlit UI components used to build the interface (sidebar, modals, viewport/metadata, live log).

Panels typically:
- render UI widgets
- read/write `st.session_state`
- call high-level functions in `app/actions.py` / services

