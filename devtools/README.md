# `devtools/` (developer tools)

Helper scripts and tools used during development and pre-publish checks.

Subfolders:
- `devtools/`: Python “devtool” scripts (smoke test, small utilities)
- `tools/`: standalone scripts/tools (EXIF, decoding helpers, etc.)

If you publish this project, ensure documentation matches what you ship (especially the smoke test scripts).
